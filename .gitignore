# Binaries
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Coverage output
*.out
*.coverprofile
coverage.txt
coverage.html

# Go workspace file
go.work

# Build artifacts
/dist/
/build/
/bin/
.air-tmp/

# Dependency directories
/vendor/

# IDE and editor files
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Temporary files
*.tmp
*.temp
tmp/
temp/

# Log files
*.log

# Environment files
.env
.env.local
.env.*.local

# Doppler local state (per-user). The committed doppler.yaml pins the
# project + config; .doppler.yaml is the local override that doppler
# writes when a user runs `doppler setup` manually.
.doppler.yaml

# Bot runtime state (conversation persistence; STATE_FILE in config.go)
state.json
state.json.old
/.claude/*
/GEMINI.md
/.cursor/*
/.playwright-mcp/*
