deploy:
  - name: Load environment variables and deploy
    image: dopplerhq/cli:latest
    command: |
      # Deploy using doppler to inject secrets into okteto deploy
      doppler run -- okteto deploy -f docker-compose.yml
